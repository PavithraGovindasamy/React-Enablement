import './Button.css'

export default function Button({ label }) {
    return (
            /** button component which has feauture of button */

        <button >
          {label}
        </button>
    );
  }
  